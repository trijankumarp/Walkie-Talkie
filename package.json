{
  "name": "walkie-talkie",
  "version": "1.0.0",
  "private": true,
  "description": "M-OS Walkie Talkie PWA — Firebase auth, Vercel deploy",
  "scripts": {
    "build": "node scripts/generate-firebase-config.js",
    "dev": "npm run build && python -m http.server 8080",
    "start": "python -m http.server 8080"
  },
  "dependencies": {
    "firebase": "^12.14.0"
  }
}