{
  "name": "mos-walkie",
  "version": "1.0.0",
  "private": true,
  "description": "M-OS Walkie Talkie PWA with Firebase auth",
  "scripts": {
    "build": "node scripts/generate-firebase-config.js",
    "dev": "python -m http.server 8080"
  },
  "dependencies": {
    "firebase": "^12.14.0"
  }
}
