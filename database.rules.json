{
  "rules": {
    "publicChannels": {
      ".read": "auth != null",
      ".indexOn": ["wifiKey"],
      "$channelId": {
        ".write": "auth != null && ((!data.exists() && newData.child('ownerUid').val() === auth.uid) || (data.exists() && data.child('ownerUid').val() === auth.uid))"
      }
    },
    "userChannels": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid"
      }
    },
    "userSettings": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid"
      }
    },
    "publicUsers": {
      ".read": "auth != null",
      ".indexOn": ["displayNameLower", "emailLower"],
      "$handle": {
        ".write": "auth != null && ((!data.exists() && newData.child('uid').val() === auth.uid && $handle === newData.child('userId').val()) || (data.exists() && data.child('uid').val() === auth.uid))"
      }
    },
    "publicUsersByUid": {
      "$uid": {
        ".read": "auth != null",
        ".write": "auth != null && auth.uid === $uid"
      }
    }
  }
}